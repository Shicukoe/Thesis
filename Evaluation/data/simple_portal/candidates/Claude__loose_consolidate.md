Create a portal homepage with a top navigation bar containing four navigation buttons labeled Dashboard, Policies, History, and Contact. Color each navigation button based on the first letter of its label: if the label starts with "H", the background should be blue; if it starts with "O", the background should be red; if it starts with "D", the background should be yellow (buttons whose label starts with any other letter keep the default style). Style the navigation buttons with rounded corners, a blue accent color, and a modern font. Add a contact form with Name, Email, and Message fields, and validate the Email field before the form can be submitted. Ensure the whole page works well and looks good on mobile screens.