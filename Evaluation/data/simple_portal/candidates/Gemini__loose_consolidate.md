<!-- Paste the Gemini "loose_consolidate" master prompt for simple_portal here. This comment line is ignored by the scorer; the file is skipped until you paste real text. -->
Create a web application project based on the following requirements, ensuring all work is done within the current project folder:

1. **Portal Homepage**:
   - Create a portal homepage.

2. **Navigation Layout**:
   - Use a top navigation bar (do not use sidebar navigation).

3. **Navigation Buttons**:
   - Add 4 navigation buttons with the following labels: **Dashboard**, **Policies**, **History**, and **Contact**.

4. **Navigation Button Styling**:
   - Style the navigation buttons to be rounded.
   - Use a modern font for the navigation.
   - Use a blue accent for the navigation styling.

5. **Conditional Button Background Colors**:
   - If the label of a navigation button starts with “H” (e.g. *History*), the background color of the button should be blue.
   - If the label of a navigation button starts with “O”, the background color should be red.
   - If the label of a navigation button starts with “D” (e.g. *Dashboard*), the background color should be “yellow”.

6. **Theme**:
   - Do not include a dark theme (only support light theme).

7. **Contact Form**:
   - Add a contact form with **name**, **email**, and **message** fields.

8. **Form Validation**:
   - The email field must be validated before the form can submit.

9. **Mobile Responsiveness**:
   - Make sure the page works well on mobile screens.
